package com.createdtr.defendtherealm.combat;

/** Movement families deliberately have separate planners and maneuver logic. */
public enum VehicleFamily {
    HOVER_AIRSHIP,
    SURFACE_SHIP,
    LAND_VEHICLE,
    FIXED_WING
}
